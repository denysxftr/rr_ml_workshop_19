# Image recognition json api
## Setup

In a separate virtualenv:

```
pip install -r requirements.txt
pip install -r requirements.dev.txt
pip install -e .
```

## Run
```
bash dev.sh
```

## Setup and run in docker
```
docker-compose build
docker-compose up
```
